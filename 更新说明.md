# 更新说明

本文件集中记录 JAV Search 的版本更新。后续发布新版本时，请将更新内容添加在文件顶部，并按更新日期和版本号由新到旧排列。

## Windows v1.4.6.28（2026-09-08）

- 同步 Docker `v1.4.6.25`～`v1.4.6.27` 的通用稳定性修复，同时保留 Windows 本机应用数据目录、本地下载器、本机文件夹选择、复制/移动归档和独立更新通道。
- 修复受限剪贴板环境下磁链无法复制的问题，增加兼容复制和完整链接手动兜底。
- 演员头像支持关闭影片目录内的 `actors` 文件夹，并增加强制重扫；Emby 同步继续使用已经下载到本机缓存的头像图片。
- “立即扫描”会先保存设置并与后台监控串行执行；任务按源文件路径保存，同番号多文件及失败状态可分别追踪。
- JAV321 不再参与首页、普通搜索和自动刮削，只保留“封面失败补救”按钮；一张有效封面即可按裁切设置生成 poster 和 fanart。
- 设置改为标准整页，保留 Windows 本机路径选择和工具检测；保存设置不再重复刷新监控。
- 修复失败刮削遗留旧 NFO 后中文翻译不生效的问题，并按当前设置安全补正受影响的归档视频和 NFO 文件名。
- FC2 常见番号写法统一进入 FC2/MissAV 专用链路；其他正常番号无码片可继续使用 JavBus、JavDB、DMM、AVSOX、AVMOO 精确兜底。
- Windows 版本继续只检查和安装 `windows-v*` Release；Docker `v*`、GHCR 镜像及 `latest` 不参与 Windows 更新判断。

---

## Windows v1.4.6.27（2026-09-04）

- Windows 版本检测改为独立读取 GitHub Releases 列表，仅筛选 `windows-v*` 发布，并包含 Windows Pre-release 通道。
- Docker `v*` 正式版与 Windows 版本不再互相参与版本比较；GHCR `latest` 继续只属于 Docker 正式版。
- 程序启动时版本区域明确显示“检测中 / 已是最新 / 检测失败”；发现新 Windows 版本时显示红点和“有更新”。
- 点击更新提示后继续使用受信任 GitHub 地址下载 Windows 安装器，核验 SHA256 后退出程序并启动安装。
- 版本筛选同时校验 Release 标签、安装器文件名和版本号一致，跳过草稿、缺少校验信息或资产错版的发布。

---

## Windows v1.4.6.26（2026-09-04）

- 修复同一下载目录包含多个不同番号时共用 `poster.jpg` / `fanart.jpg`，导致批量刮削图片交叉覆盖的问题；每个番号改用独立暂存目录。
- 详情请求与缓存增加严格番号校验，拒绝并清理返回其他番号的污染数据；前端异步详情增加结果批次保护，旧请求不再覆盖新搜索结果。
- 自动刮削增加 JAV321 精确图片检索与分辨率择优，低质量封面自动替换，并优先采用番号一致的 fanart 候选。
- NFO、poster、fanart 写入后执行有效性校验；任一主要文件失败时，任务列表明确显示失败并跳过改名和归档。
- Windows 移动归档改为先复制完整产物并校验目标，再删除源视频；归档缺件会回滚本次目标文件并保留源文件。
- “刮削失败仍归档”旧配置停止生效，自动与手动刮削统一使用失败保源规则。

---

## v1.4.6.23（2026-08-19）

- 新增 DeepL、有道、Microsoft/Bing、Google Cloud Translation 等传统翻译 API。
- 新增 DeepSeek、ChatGPT/OpenAI、Claude、Grok、Gemini 等大模型翻译渠道。
- 新增 OpenRouter、硅基流动及自定义 OpenAI 兼容中转站，支持自定义 API 地址和模型。
- 翻译请求支持复用全局代理，并可设置 5–120 秒请求超时。
- 翻译设置页改为“默认渠道 + 当前渠道配置卡片”，切换渠道时保留已填写内容，避免所有配置纵向堆叠。
- 百度、阿里云旧版配置自动兼容，升级后无需重新填写。
- 首页列表及详情页移除百度、阿里渠道切换按钮，统一为一个“翻译”按钮，由后端读取已保存的默认渠道。
- 新增嵌套 API 密钥递归脱敏和保存保护，避免设置接口返回明文或保存掩码时覆盖原密钥。
- 新增翻译适配器、默认渠道、代理及密钥保存测试，并完成既有刮削与归档功能回归。

---

## v1.4.6.22（2026-08-19）

- 番号搜索改为忽略大小写和分隔符后的完整字母数字匹配，拒绝相似但不相同的结果，并支持 `T28-304` 一类“字母+数字-数字”番号。
- 详情页样品图优先从 JAV321 获取；JAV321 无图时按需回退 JavDB。
- 群晖归档文件夹名上限收紧到 180 个 UTF-8 字节，避免中文长标题超过共享目录限制。
- 迅雷及手动放入的文件改为整目录连续稳定后再统一处理；新分段或任一文件变化会重置 5 分钟静置窗口，稳定前禁止刮削、归档和广告删除。
- 文件名中的明确番号优先于外层目录；`PART01`、`CD01` 等纯分段名继承目录番号，顶层同番号文件和嵌套分段归入同一稳定批次。
- 下载器任务路径匹配出现歧义时一律按“下载中”处理；未完成标记存在时整批暂停。
- 保留广告大小阈值：低于设定值的无番号、无分段标记候选按广告删除；明确广告标记也可删除，但 CD/PART/A-B 分段和硬字幕始终保护。
- 移动归档后仅删除完全无文件的空目录，存在任何未知文件或下载器残留时保留原目录。
- 删除已经不参与运行的“最多有效补查来源”设置；自动刮削缺封面仍固定按 JAV321 → JavDB 回退，详情页样品图继续按已启用来源执行 JAV321 → JavDB 回退。
- 广告/赠片删除与视频规整开关彻底解耦；关闭规整但保留广告删除时，仍在整批下载稳定后按大小、番号和分段保护规则清理广告。
- 设置页说明按实际执行链重新整理，明确搜索/首页/详情来源差异、搜索结果总上限、封套关闭后的 fanart 回退和最小文件大小行为。
- 移除当前版本没有执行模块的 M-Team、发种流水线和图床配置；旧 `publish_*` 目录与开关只在读取历史配置时迁移一次，随后不再进入运行配置或保存结果。

---

## v1.4.6.21（2026-08-12）

- 修复文件夹命名规则选择“只用演员”时，同名演员的不同影片归档到同一目录并互相覆盖的问题。
- 新增演员目录下二级目录命名规则：只用番号，或番号 + 标题。

---

## v1.4.6.20（2026-08-12）

- 历史归档文件修复现在会扫描归档目录内的视频和外挂字幕文件，并重新生成 `poster.jpg` 的字幕角标。
- 支持同名字幕及语种字幕文件，例如 `ABC-123.zh.srt`、`ABC-123.zh-CN.ass`、`ABC-123.chs.srt`。
- `番号-C` 硬字幕角标优先于外挂字幕角标；修复过程不联网、不修改视频和 `fanart.jpg`。
- 更新设置页说明，明确历史归档修复包含字幕角标重建。

---

## v1.4.6.19（2026-08-12）

- 修复 `番号-C/c` 硬字幕视频的自定义归档文件夹命名：涉及番号时保留 `番号-C`，标题和演员仍按原有规则拼接。
- 硬字幕与外挂字幕角标改为项目内置 PNG 素材，避免不同系统缺少中文字体导致乱码。
- 新增严格的外挂字幕识别：支持同目录同主文件名的 `.srt`、`.ass`、`.ssa`、`.vtt`、`.sub`、`.idx`，并支持 `.zh`、`.zh-CN`、`.chs` 等语种后缀。
- 角标按封面短边自适应缩放，兼容横版与竖版封面；硬字幕角标优先于外挂字幕角标。

---

## v1.4.6.18（2026-08-12）

## 修复

- 正式版本镜像构建完成后，显式将同一版本的 manifest 同步覆盖到 GHCR `:latest` 标签。
- 修复主分支构建与正式版本构建并发时，`latest` 标签可能未更新或被旧构建覆盖的问题。

---

## v1.4.6.17（2026-08-12）

## 修复

- 修复多文件种子中广告文件先完成时，下载器可能被误判为整种完成并提前删除种子的问题。
- qBittorrent / Transmission 自动删种现在以下载器报告的整种完成状态为准，不再直接依据聚合进度判断。
- 增加回归测试，覆盖进度显示 100% 但正片仍未完成的情况。

---

## v1.4.6.16（2026-08-12）

本版本修复容器升级时版本号被旧环境变量覆盖的问题：

- 不再将 `JAV_SEARCH_VERSION` 写入容器运行环境，避免 Portainer 重建时沿用旧版本号。
- 构建时把版本写入镜像内的 `/app/VERSION`，后端从镜像文件读取版本。
- 保持首页版本显示、`/api/health` 和版本更新检测功能正常。
- Release 工作流支持读取版本专属更新日志。

---

## v1.4.6.15（2026-08-12）

本版本重点修复下载完成判断与归档规整的边界问题：

- 优化 qBittorrent、Transmission 及目录实时监控下的下载完成判断，兼容下载器不添加未完成后缀、文件一开始即显示完整大小等情况。
- 增加文件稳定性与下载器状态的兜底判断，降低未完成文件被提前刮削、重命名或移动的风险。
- 收紧多段视频识别条件，避免单视频 `番号-C/c` 被误判为 CD3；不满足完整连续分段条件时保留原文件名进行规整。
- 扩展唯一番号 `番号-C/c` 的中文硬字幕识别规则，兼容同一目录中存在多个不同番号的情况。
- 保持原有单视频刮削、NFO 同名、poster/fanart 命名及广告过滤规则，无法明确判断的文件不删除、不随意改名。

---

## v1.4.6.14（2026-08-10）

- 修复直接放入下载监控根目录的单视频刮削图片冲突问题。
- 单视频会按识别出的番号创建附属目录，独立保存同名 NFO、`poster.jpg` 和 `fanart.jpg`。
- 原本带有下载文件夹的任务保持原有刮削和归档流程。
- 增加多视频同目录归档回归测试，避免图片串片或覆盖。

---

## v1.4.6.13（2026-08-09）

- 统一刮削归档图片命名为 `poster.jpg` / `fanart.jpg`，不再带番号。
- 历史归档修复支持多分段视频，为每个视频补齐同名 NFO，并清理已成功迁移的旧图片名。
- 修复 `ATID215A` / `ATID215B` 等紧贴番号的分段视频归档冲突，避免第二个视频丢失。
- 正式 tag 构建时将 tag 注入 `/api/version`，前端版本检测与镜像版本保持一致。

---

## v1.4.6.12（2026-08-09）

- 首页普通卡片不再自动追加 JavDB 详情请求；JavBus、FC2、AVSOX 等来源使用各自的详情数据。
- JAV321 保留为点开详情时的按需样品图补充来源。
- JavDB 作为主来源且列表缺少封面时，仅对当前可见页执行一次封面兜底请求。
- 首页任务状态改为表格展示，区分当前阶段、总状态、刮削结果、归档结果和错误说明。
- 缩小任务页黄色操作按钮，减少视觉突兀感。

---

## v1.4.6.11（2026-08-09）

- 自动刮削改为 JavBus 优先，缺少封面时依次使用 JAV321、JavDB 兜底。
- 自动刮削不再请求 JAV321 样品图；JAV321 样品图仍仅在详情页按需获取。
- 已取得封面时，按裁切设置生成 poster/fanart，不再为寻找独立 fanart 执行多源补查。
- 移除归档后 poster/fanart 补全的后台轮询和持久化入队入口，降低空闲扫描负担。
- 首页最新来源配置保持不变，继续支持 JavDB、FC2、AVSOX、AVMOO 等来源，不加入 JAV321。

---

## v1.4.6.10（2026-08-09）

- 归档配套文件命名修复改为手动执行，新增“手动同步 NFO/图片文件名”按钮。
- 扫描归档目录中的历史 NFO、poster、fanart，将其同步为视频文件名；视频文件本身保持不变。
- Emby 通知仍限定为当前影片目录，不触发全库刷新；未增加任何群晖索引调用。

---

## v1.4.6.9（2026-08-09）

- 新增任务状态页，顶部任务按钮位于设置按钮之前，主内容区可在搜索结果与任务列表之间切换。
- 按番号聚合下载、刮削、归档和失败状态，任务页打开时自动同步运行进度。
- 失败任务保留更长时间；同一番号重试后会更新原记录，成功状态覆盖此前失败状态。
- 清理操作仅删除任务日志摘要，不会停止后台任务或删除媒体文件。

---

## v1.4.6.8（2026-08-08）

- 修复选择“番号 + 标题 + 演员”归档命名时，翻译标题过长导致文件夹创建失败的问题。
- 按当前文件系统的单项名称限制自动计算可用长度，并优先保留番号和演员名称。
- 标题截断优先使用句号、逗号作为分界，无法分界时按字符截断并追加省略号。

---

## v1.4.6.7（2026-08-08）

## Emby 演员头像同步

- 同步链路简化为只从当前影片 `People` 关联取得 Person ID，不再查询或分页枚举 Emby 全局人物库。
- 始终读取并上传已经下载到归档影片 `actors/<演员名>.jpg` 的本地图片字节；NFO `<thumb>` 继续只作为元数据与回退地址。
- 初次归档只定向通知当前影片并短暂等待 Emby 建立影片演员关系，不执行全媒体库刷新。
- 精确影片路径未命中时按番号定位当前影片，兼容 Emby 对容器挂载路径的标准化。
- 影片或演员关系尚未建立时，将仍待上传的演员写入持久化队列，容器重启后继续处理；已经成功的演员不会重复排队。

## 后台任务负担

- Emby 延迟重试不再重复发送媒体入库通知，每轮只进行一次当前影片定位和必要的本地头像上传。
- 重试采用 2、5、15、30、60 分钟的渐进间隔，之后每 6 小时低频尝试，直到本地头像成功写入 Emby。
- 无头像的 JavDB 待补任务仍默认每 12 小时读取一次演员目录；无待补演员时不发起请求，一次页面读取批量匹配全部待补项。

## 验证

- 新增当前影片 `People` 时序、日文名字标准化、路径标准化、持久化重试和后台单轮查询测试。
- 验证 Emby 上传内容来自本地演员图片字节，延迟重试不重复通知媒体库。
- 92 项自动化测试、Python 编译检查及差异格式检查全部通过。

---

## v1.4.6.6（2026-08-08）

## 演员头像低频增量补全

- 影片首轮仅使用 JavBus、AVSOX 等现有低成本来源，取得有效头像后立即结束。
- 首轮仍缺头像的演员写入持久化待补队列，不在影片刮削过程中反复请求需要过盾的 JavDB。
- 后台默认每 12 小时读取一次 JavDB 演员目录，只下载与待补演员匹配的头像；启动后先等待完整周期，避免与首页或手动任务争抢 FlareSolverr。
- 命中后更新全局 `actor-cache`、所有关联影片的 `actors/演员名.jpg` 和 NFO，再定向通知相关影片目录。
- Emby 同步从当前影片 `People` 关联取得 Person ID，上传本地头像字节并验证 Primary ImageTag。
- 设置页可开关后台任务，并可将周期调整为 1–168 小时；普通手动演员任务也可设置请求冷却时间。

## 刮削与归档规则

- 新增“只用演员”文件夹命名；多人模式最多使用前 7 位演员，没有演员时安全回退为番号。
- 新增保留原视频文件名开关；关闭重命名后，NFO、图片、演员头像及归档仍照常执行。
- 新增封套图片模式：完整横向封套作为 fanart，并从中缝左侧少量位置开始裁取右侧正面作为 poster；关闭后继续使用独立样品/宣传图作为 fanart。

## 设置页整理

- 移除已确认无效的 fc2ppvdb 抓取页数控件；后端继续保留旧配置键以兼容已有配置。
- 更新刮削、规整、视频改名、广告删除、自定义目录、演员补查及 Emby 同步说明。
- 去除设置分组中的历史版本号标记，并更新版本显示兜底值。

## 验证

- 新增 JavDB 演员目录解析、仅缺失头像入队、自定义演员目录、封套模式和请求节流测试。
- 88 项自动化测试、Python 编译检查及差异格式检查全部通过。

---

## v1.4.6.5（2026-08-07）

## Emby 演员头像

- 定向通知当前影片目录后，如果 Emby 的全局 Person 索引暂时找不到同名演员，会继续从刚导入的影片条目读取 `People` 关联并取得 Person ID。
- 头像同步仍然读取并上传已经下载到影片目录的 `actors/<演员>.jpg` 本地字节；NFO 中的远程 `<thumb>` 只作为元数据与回退地址。
- 避免影片已经识别出演员、但全局人物索引尚未更新时误报“Emby 中未找到同名演员”。

## 横向封套裁图

- 完整横向 DVD 封套改为从中缝左侧少量位置开始，截取右侧约 53% 的区域。
- 新裁剪会保留中缝附近的画面与文字，并包含完整右侧正面，避免旧版固定 `高度 × 2/3` 的裁法把封面裁窄。

## 验证

- 新增从当前影片 `People` 关联反查演员 ID 的自动化测试。
- 更新封套裁剪范围测试；完整回归测试通过。

---

## v1.4.6.4（2026-08-07）

## 演员头像与 Emby 验证

- Emby 演员头像继续上传影片目录中已经下载的 `actors/<演员>.jpg` 本地字节。
- 上传前后对比人物 `Primary ImageTag`；已有标签未变化时不再误报更新成功，并在结果中记录新旧标签便于诊断。
- 兼容不返回 ImageTag 的旧版 Emby，继续使用 Primary 图片存在性验证。

## Poster 与 Fanart

- 仅当来源及图片 URL 均能确认是 JavBus/JavDB 主封套，且真实尺寸符合完整横向 DVD 封套特征时，裁取右侧正面并保存为标准 `2:3` 竖版 poster。
- 样品图、详情图库、未知来源图片、已有竖版封面及普通 `16:9` 图片不会进入封套裁图。
- 主封套仍优先复用首页/详情图片的内存与磁盘缓存；裁切发生在取得缓存字节之后，不增加上游请求。
- fanart 最多检查八张独立样品图，只接受尺寸不少于 `640×360`、宽高比不低于 `1.35` 的横图；不再复制 poster 或误用竖图、封套及缩略图。

## 依赖与验证

- 新增 Pillow 依赖，用于本地无损流程中的封套正面裁取与 JPEG 输出。
- 图片类型、裁图、Emby ImageTag、多演员同步及既有归档流程均有自动化回归测试覆盖。

---

## v1.4.6.3（2026-08-07）

## 详情页与图片修复（2026-08-07）

- 修复首页条目详情抓取失败或超时后仍被错误标记为“已加载”，导致再次点开也不重试的问题。
- 打开详情页后立即展示已有内容，并行补抓主来源详情与其他图片；补图优先使用直连、图片更丰富的 JAV321，再尝试需要过盾的 JavDB。
- 新增按番号解析指定来源详情的接口，即使首页合并结果没有 JAV321 详情 URL，也能按番号补抓。
- 扩充 JAV321 当前页面解析，支持宽栏图库、懒加载图片、`picture/source`、原图链接及多张样品图，并过滤缩略图重复项。
- JAV321 网络错误和非 200 响应不再伪装成“没有结果”，保留后续重试机会。
- JavDB/FlareSolverr 详情与补图超时预算放宽到 75 秒，兼容浏览器启动、过盾及认证代理会话。

## 规整复用

- 点击详情后取得的样品图、磁力、评分人数及各来源详情 URL 会随推送元数据按 infohash 持久化。
- 下载完成后规整优先使用推送时已经取得的图片 URL，并复用首页/详情图片代理的内存与磁盘缓存，避免重复请求上游。
- 若影片没有点开详情，或详情未取得独立样品图，则继续执行原有的有限来源补图流程。

## 兼容性

- 不改变演员头像归档及 Emby 同步规则；Emby 仍上传本地 `actors/<演员>.jpg` 文件内容。
- 主标题与封面仍以主来源为准，JAV321/JavDB 仅补充缺失的简介、演员、标签、样品图、磁力与评分等字段。

---

## v1.4.6.2（2026-08-07）

## 修复性更新（2026-08-07）

- 修复带账号密码的主代理传给 FlareSolverr 后实际不可用：认证代理现在按官方接口先创建代理浏览器会话，再请求 JAVDB，并在完成后销毁会话。
- 「测试 JAVDB 连通」改为通过 FlareSolverr 浏览器测量真实出口 IP，避免把后端容器的出口误报成 FlareSolverr 出口。
- 扩充 JAVDB 当前详情页图片解析：支持竖版高清封面、gallery 灯箱、懒加载 `picture/source` 与多张样图，并自动去重。
- 保持原有演员头像行为不变：NFO 的 `<thumb>` 仍是来源元数据，Emby 最终上传本地 `actors/<演员>.jpg` 文件内容。

## 主要更新

- 收紧多视频分段判断：仅在存在明确 `CD1/CD2` 等分段证据时生成分段后缀，避免把广告片或同内容的不同清晰度版本误判成分段。
- 对少数疑似同片多清晰度场景，只有在系统已提供 `ffprobe` 时才按需比较时长、分辨率和编码；项目与容器不捆绑 ffmpeg，证据不足时保留源文件。
- `poster` 与 `fanart` 改用来源网站提供的两张独立图片，不再复制同一图片，也不在本地裁切；没有独立背景图时不伪造 fanart。
- 图片缺失时按需串行补查其他已启用来源，复用详情缓存、命中即停，默认最多补查 2 个来源，可在设置中调整或关闭。
- 新增默认关闭的 JAV321 与 DMM/FANZA 来源；DMM/FANZA 仅调用官方联盟 API，需要配置 API ID 与 Affiliate ID。

## 兼容与安全

- 不修改现有演员头像归档及 Emby 同步规则，最终仍上传本地 `actors/<演员>.jpg` 文件内容。
- 未增加 ffmpeg 或其他媒体解析依赖，不增加 Docker 镜像的固定运行负担。
- 无法可靠判断的视频不会强行改名为 CD 分段，也不会覆盖或删除不确定的源文件。

---

## v1.4.6.1（2026-08-03）

本版本以本地 v1.4.5 为基线，未引入 v1.5 的发种中心等无关功能。

## 主要更新

- 将刮削、规整、归档拆分为三个独立开关。
- 引入保守的视频分类机制：唯一视频、最大视频、达到保底大小的视频、可识别番号及分段视频一律保留。
- 支持 `番号`、`番号 + 标题`、`番号 + 演员`、`番号 + 标题 + 演员` 四种归档文件夹命名；标题可单独选择翻译，多演员可选第一位或全部。
- `番号 + 标题 + 演员` 模式严格分字段处理：只翻译标题，演员名始终保留刮削源原名，不会把标题与演员拼接后整体翻译。
- 修复归档目录位于下载监控目录内（例如监控 `new`、归档 `new/jav`）且选择“移动”时，归档成品被再次扫描并可能被同路径移动删除的问题；现在监控硬性排除归档子树，文件传输层也拒绝删除同一路径。
- 全面加固硬链接、复制、移动三种归档模式：已有目标绝不覆盖；复制先写目标侧临时文件再独占落位；移动仅在目标完整落地并校验大小后删除源文件；传输失败保留源文件并清理临时文件。
- 移动时若源文件无法删除，日志会按系统错误码区分“被程序占用/锁定、权限或 ACL 拒绝、只读属性、只读挂载、设备忙、文件被外部移动”等原因，给出处理建议并记录安全回滚结果；无法判断时保留原始异常和 errno/winerror。
- 针对群晖及 Linux/NAS 环境增加现场诊断：检查父目录写入/遍历权限与 uid/gid、容器视角只读挂载、immutable/append-only 属性、`/proc/locks` 建议锁、可见进程打开句柄，以及 NFS/CIFS 的 `ESTALE/EIO/EREMOTEIO/ENOTCONN/ETIMEDOUT` 等异常；日志会注明普通 Linux 打开句柄通常不会阻止删除，避免误判根因。
- 移动后的目录清理改为只要还存在任何视频（不限大小、命名或完成状态）就保留整个目录；目录扫描失败同样取消清理，不再把“检查失败”误判为“目录为空”。
- 广告/赠片视频删除拆为独立开关并默认开启；用户可单独关闭，关闭后规整仅负责命名、不自动删除视频。
- 修复手动单文件刮削的归档调用缺少番号参数、归档方式未生效及成功状态字段读取错误。
- 演员模式继续合并全部已启用来源，以获得尽可能多的结果；卡片保留来源标识，详情加载后明确显示“单体作品”或“多人作品”，便于区分与 JavDB 页面口径的差异。
- 演员头像会按番号从 JavBus 详情补查，并先缓存到影片可见目录 `actors/<演员>.jpg` 后随影片归档；配置 Emby people 路径后，再同步到 `metadata/people/<演员>/folder.jpg` 与 `portrait.jpg`。旧版 `.actors` 缓存归档时会自动合并到 `actors`。
- 演员头像现为独立任务：按“本地缓存 → NFO 头像 → 番号逐源补查 → JavBus 姓名回退”的顺序处理，补齐后立即停止后续来源；AVMOO/AVSOX 自动去重，JavDB 因不提供头像自动忽略。支持全局缓存、批量扫描、强制更新、NFO 回写和刮削后自动运行。
- 新增 Emby API 设置、连接测试和自动同步；头像刮削成功后按姓名精确匹配 Emby Person，并上传为演员 Primary 主图。API Key 保存后脱敏显示。
- 修复同一页面多个资源抽屉重复使用按钮 ID，导致第二个“推送下载”请求成功但 UI 更新到其他隐藏按钮的问题；现在直接更新实际点击的按钮节点。
- 适配 AVSOX 2026-06 搜索 API 参数变更，将 `page`、`pageSize` 移入搜索对象，修复 `api search code=400` 及演员头像无法补全。
- 修复 JavBus 当前页面将演员名与头像拆分为兄弟节点后，旧选择器只能取得演员名、头像 URL 始终为空的问题；按演员链接关联头像并以演员 ID 路径兜底，同时自动绕过旧版留下的空头像详情缓存，无需手工清理 `/config/detailcache`。
- 核对并强化多演员头像流程：NFO 中每个 `<actor>` 均独立下载、缓存并同步 Emby；重复演员会按规范化姓名去重，`nowprinting/noimage` 等占位图不再被误存为演员头像或计入成功数。
- 完善 Emby 首次入库时序并修复同步过早问题：头像先下载并随影片归档，归档成功后才调用 `/Library/Media/Updated` 定向通知当前影片目录，轮询等待新 Person 创建，再批量上传 `/Items/{Id}/Images/Primary` 并验证；不再调用会扫描全部媒体库的 `/Library/Refresh`。支持单独配置 Emby 容器内归档根路径，解决两个容器挂载路径不同的问题。
- 修复演员头像命中本地缓存时丢失原始 URL、导致影片 NFO 没有 `<actor><thumb>` 的问题：优先从全局缓存元数据恢复，旧缓存缺少元数据时仅补查 URL 而不重复下载图片，并在日志中显示 NFO URL 实际写入数量。
- 增强 Emby 日文演员匹配：统一全角/半角片假名、兼容字符、隐藏空白与横线变体；`SearchTerm` 因日文索引未命中时，自动通过按名称接口及分页人物列表进行规范化精确匹配，避免人物已显示但本地头像未上传。
- 修正规整后偶发只归档视频、漏带 NFO/封面的情况；附属文件改为大小写无关匹配并明确随视频落地。
- 自动获取公共 Tracker best 列表，缓存七天；失败沿用旧缓存或内置兜底；支持用户自定义和手动刷新。
- qBittorrent 与 Transmission 推送磁力时自动补充不重复的 Tracker，并在加种后 reannounce。
- 资源搜索默认直连 `https://sukebei.nyaa.si/`，无需配置 Jackett；Jackett 改为可选增强，失败或无结果时自动回退 Sukebei。新增 `/api/resources/search`，旧 `/api/jackett/search` 保留兼容。
- 下载完成判断改为读取 qBittorrent/Transmission API：任务必须同时满足进度 100% 与剩余字节为 0；不再用磁盘大小、mtime 或临时后缀推断下载器任务，兼容预分配和关闭临时后缀。只有手动放入、无法匹配任何下载器任务的文件才使用静置/大小稳定兜底；下载器已配置但离线时暂停自动处理。

## 安全说明

- 判断不明确时保留文件，不删除。
- 规整关闭后不删除任何广告或赠片视频。
- 整目录清理前，存在达到保底大小的未处理视频时禁止删除目录。
- 公共 Tracker 参数可由调用方关闭，私有/PT 种子不得补充公共 Tracker。

---

## v1.4.6-beta（2026-08-01）

本版本以本地 v1.4.5 为基线，未引入 v1.5 的发种中心等无关功能。

## 主要更新

- 将刮削、规整、归档拆分为三个独立开关。
- 引入保守的视频分类机制：唯一视频、最大视频、达到保底大小的视频、可识别番号及分段视频一律保留。
- 支持 `番号`、`番号 + 标题`、`番号 + 演员` 三种归档文件夹命名；标题可单独选择翻译，多演员可选第一位或全部。
- 演员头像会按番号从 JavBus 详情补查，并先缓存到影片可见目录 `actors/<演员>.jpg` 后随影片归档；配置 Emby people 路径后，再同步到 `metadata/people/<演员>/folder.jpg` 与 `portrait.jpg`。旧版 `.actors` 缓存归档时会自动合并到 `actors`。
- 演员头像现为独立任务：可按 NFO 演员姓名依照 JavBus、AVSOX、AVMOO、JavDB 的自定义优先级逐个补图；NFO 无演员时可按番号合并演员名单。支持全局缓存、批量扫描、强制更新、NFO 回写和刮削后自动运行。
- 修正规整后偶发只归档视频、漏带 NFO/封面的情况；附属文件改为大小写无关匹配并明确随视频落地。
- 自动获取公共 Tracker best 列表，缓存七天；失败沿用旧缓存或内置兜底；支持用户自定义和手动刷新。
- qBittorrent 与 Transmission 推送磁力时自动补充不重复的 Tracker，并在加种后 reannounce。
- 资源搜索默认直连 `https://sukebei.nyaa.si/`，无需配置 Jackett；Jackett 改为可选增强，失败或无结果时自动回退 Sukebei。新增 `/api/resources/search`，旧 `/api/jackett/search` 保留兼容。
- 下载完成判断改为读取 qBittorrent/Transmission API：任务必须同时满足进度 100% 与剩余字节为 0；不再用磁盘大小、mtime 或临时后缀推断下载器任务，兼容预分配和关闭临时后缀。只有手动放入、无法匹配任何下载器任务的文件才使用静置/大小稳定兜底；下载器已配置但离线时暂停自动处理。

## 安全说明

- 判断不明确时保留文件，不删除。
- 规整关闭后不删除任何广告或赠片视频。
- 整目录清理前，存在达到保底大小的未处理视频时禁止删除目录。
- 公共 Tracker 参数可由调用方关闭，私有/PT 种子不得补充公共 Tracker。

---

## v1.4.4（2026-06-10）

在 V1.4.3 基础上**专项优化 FC2**，让「最新片源」真正拿到市面最新、且详情更快更省。其它功能与配置不变。

## 1. FC2 最新片源接入 sukebei（解决「不够新」）

**问题**：fc2ppvdb 的「新着列表」会封顶在某个号（实测只到 ~489 万），够不到市面最新；且它首页不支持翻页、忽略 `per_page`，最新页还把「最新 + 人気老片」混排。

**优化**：

- FC2 最新改为**优先用 sukebei 发现番号**（种子站按 id 倒序＝最新、**直连不过盾、最快**），能拿到 fc2ppvdb 够不到的最新号；sukebei 够量时直接跳过较慢的 fc2ppvdb 首页。
- 抓回后**统一按 FC2-PPV 编号降序**取最新 N 条（编号越大越新），老热门片自动沉底。
- sukebei 挂了 / 关闭时，自动回退 fc2ppvdb 首页兜底。
- 新增 `backend/scrapers/_sukebei.py`。

## 2. 后台预抓 MissAV（标题/封面/样品图，低负担）

sukebei 卡片初始只有种子标题 + 封面。新增**后台串行 + 节流 + 直连 MissAV**（不过盾）慢慢抓最新片的**真标题 / 封面 / 样品图**灌入缓存：刷新后列表卡标题/封面变干净、点开详情**样品图秒出**。默认开、负担极低，可在设置里关闭或调条数。

## 3. FC2 详情改为 MissAV-only（点开即出、不再过盾）

FC2 无女优/标签需求，而 fc2ppvdb 在详情里唯一独有的就是这些、对最新片还常为空，却要花一次慢速过盾。**详情移除 fc2ppvdb，只用 MissAV**（标题/封面/样品图，命中预抓缓存则秒出），**彻底不碰 FlareSolverr**，也不再与 JavDB/刮削抢实例。fc2ppvdb 仍保留用于**关键词/女优名搜索**与**首页最新兜底**。

## 4. 新增设置（FC2 数据源分栏）

- 最新优先用 sukebei（默认开）
- 后台预抓样品图（默认开）+ 预抓条数（默认 20）
- fc2ppvdb 抓取页数（兜底用，默认 1）

## 涉及文件

- 新增 `backend/scrapers/_sukebei.py`
- `backend/scrapers/fc2.py`、`backend/config_manager.py`、`backend/main.py`、`frontend/index.html`

> **本版已转正（`V1.4.4`，Docker `:latest`）**。经 beta 验证稳定后发布为正式版。

---

## v1.4.3（2026-06-07）

> **本版已转正（`V1.4.3`，Docker `:latest`）**。经 beta 验证稳定后发布为正式版。

> 📌 **本文档含 beta 期内的覆盖修订**：FlareSolverr 改回「填 URL 即用」（不再内置进安装包）、
> 新增「防过载死机」三层保护、修复 JavBus+JavDB 合并卡详情页样品图。详见下方「FlareSolverr」与「修订记录」两节。

> 🆕 **转正版（V1.4.3）相对 beta 的新增/修复**：
> ① **FC2 列表页直接出封面 + 标题**——本地解析跳过懒加载占位图、回退 MissAV 确定性封面（零额外 FlareSolverr 负担），标题缺失时直连 MissAV 补全（禁用 FlareSolverr 回退、带缓存）；
> ② **详情页样品图改走后端图片代理**——点开大图也带正确 Referer + 出口代理抓取，客户端本机无代理也能查看（所有数据源通用）；
> ③ **FlareSolverr 取页闸新增优先级**——点详情的交互请求可插到「首页最新/搜索」的批量任务前面（单实例下无法抢占正在执行的请求，但可排到队首），并为交互类预留队列额度，繁忙时点详情不被「排队已满」挡掉。

## 一句话

① 新增 **FC2-PPV 专用数据源**（fc2ppvdb.com），把 FC2 素人/无码片源纳入检索与刮削；
② 首页顶部新增**版本号 + 更新检测**；③ 首页最新片源改为**各来源并行、边抓边显示**，
并提供**数据源切换标签**快速筛选；④ **FlareSolverr 回到「填 URL 即用」** + **防过载死机保护**。

---

## FlareSolverr：填 URL 即用（不内置进安装包）

FlareSolverr **不打进本项目的安装包**——自行在任意主机跑一个，再到「设置 → JavDB 反爬 / FC2 数据源」
填它的 URL 即可（`http://192.168.1.100:8191` 或外网域名 `https://fs.你的域名.com`）。简单、解耦、好排查。

```bash
# 自己单独跑一个 FlareSolverr（端口默认 8191）
docker run -d --name flaresolverr --restart unless-stopped \
  -p 8191:8191 -e LOG_LEVEL=info -e TZ=Asia/Shanghai \
  ghcr.io/flaresolverr/flaresolverr:latest
```

### 留空＝自动探测（零填写，装了就连）

地址栏**留空即自动探测**本机/同宿主机已部署的 FlareSolverr，探到就用、探不到回退直连，**不用再手查容器 IP**：

- **探测顺序**：容器名 `flaresolverr:8191` → `host.docker.internal:8191` → 网桥网关 `:8191` →
  `127.0.0.1:8191` → **扫描本容器所在 /24 网段的 8191**。
- 最后那步「网段扫描」是关键——默认 bridge 网络没有容器名 DNS、主机防火墙又常挡「网桥→宿主机网关」，
  这时唯有**容器对容器直连**最稳，扫描能自动找到 `172.17.0.x` 这种 sibling 容器 IP。
- **探测方式（两段式、快且轻）**：常见地址发轻量 `GET /`；网段扫描先**全并发 TCP 连扫**
  （一个超时窗口 ~0.8s 扫完整段，而非分批），只挑出极少数 8191 开放的 IP，再对它们 `GET /`
  确认响应含 `FlareSolverr is ready`。相比「对全网段逐个发 HTTP」既快得多、又几乎不产生无谓请求。
- **不死循环**：探到的地址正缓存 5 分钟直接复用；**探不到也负缓存 5 分钟，期间不再全网段重扫**，
  自动回退直连——既不拖慢日常搜索，也不会无限重试。点「测试连通」会强制重新探测。
- **回填地址框**：「测试连通」探到地址后，会把它**直接填进上方 FlareSolverr 地址框**，
  点「保存设置」即可固定该地址（留空则保持每次自动探测）。
- 想固定地址的，照常在框里手填具体 URL，**优先级最高**（填了就不再自动探测）。

> 这条专为「pull + docker run」的用户准备：自己起一个 FlareSolverr（`-p 8191:8191`），
> 本应用地址栏**留空**即可，多数情况下自动就连上了，无需理解 docker 网络。

JavDB / FC2 / MissAV 统一走 `scrapers/_fsgate.py` 的共享取页层：

- **填法全兼容**：缺 `http://` 自动补、不填端口默认 `8191`、带 `/v1` 或末尾斜杠自动清理。
- **快失败 + 缓存**：连接超时压到 6s（死地址不再卡 70s），连上的地址会被缓存，后续请求直接命中。
- **诊断更直白**：连通诊断回显「实际连通地址」；全连不上时列出已尝试的地址与排错提示。
- 容器内误填 `localhost`/`127.0.0.1` 时仍会兜底尝试 `host.docker.internal` / 网桥网关；局域网 IP / 域名一律不改写。
- `config_manager` 仍保留可选的 env 兜底（`JAVDB_FLARESOLVERR_URL` / `FC2_FLARESOLVERR_URL`，设置页留空时生效），
  但**安装包不再预置**，填不填全看自己。

> ⚠️ 若 FlareSolverr 跑在机房 VPS（数据中心 IP），JavDB 仍可能按 IP 信誉**硬封 403**——
> 需在「设置 → 主代理」填一个【**非日本**】住宅代理（保持「FlareSolverr 复用主代理」开启）。

### 防过载死机（三层保护）

FlareSolverr 是单浏览器实例、一次只处理一个请求。之前后台刮削/首页最新/搜索/连通测试同时怼，
请求在它内部堆积、互挤到集体超时，最终把实例卡死（被误判成「IP 失效」）。`_fsgate.py` 三层根治：

1. **串行闸** `Semaphore(1)`：一次只向 FlareSolverr 发一个请求。
2. **背压（排队上限 16）**：排队 + 执行中超过上限就**立刻快失败**，杜绝无限堆积（堆积正是压垮单实例的根因）。
3. **熔断**：连续 4 次「连不上 / 读超时」即开闸冷却 45s，期间快失败、不再怼半死的实例；
   实例**应答了 JSON（哪怕站点 403）算健康、不触发熔断**，只有实例自身卡死才计入；一次健康应答即清零。
   另含两次请求间 0.4s 最小间隔，给单浏览器留会话回收时间。

> 本保护只保证「**不被自己人挤死**」；更高吞吐仍需多开实例。

---

## 首页体验增强（本版新增）

### 1. 版本号与更新检测

- 顶部搜索框右侧显示当前版本 `v1.4.3`。
- 后端新增 `GET /api/version`，**由后端代理访问 GitHub** 取最新 release 并**缓存 1 小时**
  （可走配置代理出网，群晖内网也能用，避免浏览器跨域 / 被 GitHub 限流）。
- 检测到更高版本时，版本号旁出现**红点脉冲**提醒，点击跳转 GitHub release 页。
- 比较为语义化比较，自动忽略 `V` 前缀（`V1.4.3` ↔ `1.4.3`）。

### 2. 最新片源「边抓边显示」

- 旧版 `/api/latest` 一次性等所有源合并完才显示；本版前端对**每个来源并行请求**
  `GET /api/latest?source=xxx`，**谁先回来谁先显示**，慢源（如走 FlareSolverr 的 JavDB/FC2）
  随后按番号去重补入，不再被慢源拖住整页。
- 状态栏实时显示进度（如「最新片源加载中（1/3） · 已到 JavBus」）。
- 合并按番号去重，多源命中的卡片合并来源标记，且**不重排**以保证详情按需补全的索引稳定。

### 3. 数据源切换标签

- 列表上方出现数据源筛选条（如 `全部 / JavBus / JavDB / FC2`，各带条数），点击只看该来源。
- 合并卡（如 JavBus+JavDB 同番号）在所属的每个来源下都会出现。
- 筛选条在**首页最新与搜索结果**下均生效（仅单一来源时自动隐藏）。

---

## MissAV 补全 FC2 封面/标题（本版新增）

fc2ppvdb 对**已下架**的 FC2-PPV 常只剩番号骨架（无封面/标题）。本版接入 **MissAV** 作为
FC2 的补全源——它对 FC2 覆盖很好，连下架条目都保留**完整标题 + 高清封面**（封面走
`fourhoi.com` CDN）。

- **列表卡封面**：fc2ppvdb 缺封面时，直接用 MissAV 的**确定性封面 URL**
  （`https://fourhoi.com/fc2-ppv-{番号}/cover-n.jpg`）兜底——**零额外请求**，404 由前端图片代理
  优雅降级为无封面。
- **详情标题/封面**：打开详情时**并发**抓 fc2ppvdb 与 MissAV，用 MissAV 补 fc2ppvdb 缺失的
  标题/封面（几乎不增加延迟）。补全后来源标记为 `FC2 + MissAV`。
- **样品图**：MissAV 无传统图廊，但播放器进度条带一组**逐帧截图**
  （`https://{cdn}/{uuid}/seek/_N.jpg`，CDN 如 nineyu.com，约 10+ 张）——这些就是可用的样品图，
  已并入 FC2 详情的「样品图」区块（与 JavDB 样品图同款展示）。另带 `preview.mp4` 预览视频
  字段（确定性 URL，前端暂未渲染，留作后续动态预览）。
- 这些 MissAV 系 CDN（fourhoi/nineyu/surrit/sixyik）**有防盗链**，需带 `Referer: missav` 域名；
  已在图片代理 `/api/img` 增加对应 Referer 规则，前端无感。
- 只取 MissAV 的**标题 + 封面 + 样品图**（高可靠）；不抓其女优/体裁（其导航里混有
  「女優ランキング」等噪声，难以可靠区分）。FC2 素人片本就少有结构化女优/体裁。
- 设置页「FC2 数据源」组新增「MissAV 补全」开关（默认开）与镜像填写框（域名常变，可填多个
  逗号分隔做兜底）。

> 关于 Jackett 里看到的标题：那是**种子发布名**（sukebei 上传者手填），非结构化元数据，
> 所以带 `+++`、特典等噪声。本版的标题/封面来自 MissAV，干净规整。

## FlareSolverr 并发治理（修复批量详情超时）

引入 FC2（同走 FlareSolverr）后，翻页时**批量补全整页详情**会把 6 个并发请求同时砸向
FlareSolverr。而 **FlareSolverr 是单浏览器实例、不支持并发**，请求互相排队挤到**集体超时**
（日志里成片的 `[enrich] javdb/fc2 ... timeout`）。注意：这不是 IP 失效——单请求的连通测试/
点开单条详情仍正常。两处修复：

1. **后端串行闸**（`scrapers/__init__.py`）：走 FlareSolverr 的来源（JavDB/FC2）改走进程级
   `Semaphore(1)` **全局串行**，一个一个来；直连来源（JavBus/AVSOX/AVMOO）仍并发。
2. **前端慢源不批量预取**（`index.html`）：配了 FlareSolverr 时，JavDB/FC2 详情**不参与翻页
   批量预取**，改为**点开详情时单条按需补全**（`ensurePageDetails` 跳过慢源，`openDetail →
   ensureItemDetail` 兜底）。合并卡（主源 JavBus）不算慢源，照常预取，其 JavDB 样品图/磁力仍
   点开时按需补抓——既不压垮 FlareSolverr，又不丢信息。

> 经验：FlareSolverr 单实例吞吐有限，慢源详情天然只能「逐条按需」。若要批量更快，需多开
> FlareSolverr 实例或换更快的过盾方案。

3. **全局取页层串行闸**（`scrapers/_fsgate.py`）：上面的串行闸只管 `enrich`（详情批量），
   而**搜索/诊断/最新**走的是另外的路径没保护——后台刮削搜 JavDB 占用 FlareSolverr 时，前台点
   「JavDB 连通测试」会排队挂起直到 `ReadTimeout`（典型误判为「IP 失效」，实则是撞车）。
   修复：新建进程级 `GATE = Semaphore(1)`，JavDB/FC2/MissAV 的 FlareSolverr 取页（POST）**全部
   通过这一把闸全局串行**，一次只发一个请求。诊断会「排队等待」而非「撞车超时」。
   注意：这只能消除撞车；**机房 IP（如 Cogent）过 JavDB 盾本就难，根治需换非日本住宅 IP**。

## 无码番号识别修复（library.py）

刮削把 `hhd800.com@060226_01-10MU` 误识别成 `HHD-800`。双缺陷：① 真实番号 `060226_01`
（10musume `MMDDYY_NN`）不在番号正则里，匹配失败；② 失败后回退去匹配**未去广告的原始名**，
抓到广告域名 `hhd800` → `HHD-800`。修复：

- **回退也剥广告域名**：`_code_from_name` 回退时用 `_SITE_NOISE` 清理过的串，广告域名不再被当番号。
- **增加无码番号正则**：`\d{6}[-_]\d{2,4}`（10musume/1pondo/Caribbean 的 `060226_01`/`060226-001`/
  `060226_001`）、`[A-Z]{3,10}-\d{3,5}-\d{2,4}`（heydouga-4017-001，放在普通字母番号前防截断）。
- 有码番号（ABP/SSIS/390JAC/FC2 等）不受影响；广告名不再被瞎猜成番号。

> 注意：识别正确 ≠ 能刮到元数据。刮削目前只搜 javbus/javdb，10musume 等无码厂牌不在其上，
> NFO 元数据可能仍为空——需后续给刮削接入无码数据源才能解决。

## 修订：JavBus+JavDB 合并卡详情页样品图/磁力（beta 内修复）

后端 `javdb.py` 与 1.4.2 **字节级一致**，样品图/磁力的按需补抓与展示函数
（`detailNeedsJavdbExtra` / `renderDetailExtra` / `ensureJavdbExtras`）行为不变。
另把 `enrich` 单条超时放宽规则从「仅 JavDB」扩展到「JavDB + FC2」（都走 FlareSolverr 需更长超时）。

**但 beta 内发现一个真 BUG**：单独 JavDB 卡详情正常，**JavBus+JavDB 合并卡详情页出不来样品图**。
根因——合并卡按需补抓 JavDB 样品图/磁力依据 `source_urls['JavDB']`：

- **搜索**走后端 `_merge_lists`，本来就写了 `source_urls` → 正常；
- **首页最新**是各源并行抓、**前端** `mergeLatestBatch` 按番号合并，这段**漏写了 `source_urls`**，
  合并卡缺 `source_urls['JavDB']`，`detailNeedsJavdbExtra` 判为「无需补抓」，样品图永远不出来。

**修复**：`frontend/index.html` 的 `mergeLatestBatch` 新增/合并条目都补齐 `source_urls`；
`backend/scrapers/__init__.py` 单来源返回也补 `source_urls` 默认值兜底，保证后续合并不丢自己的详情页 URL。

---

## 修订记录（覆盖 1.4.3 beta，不另起版本号）

| 改动 | 文件 |
|------|------|
| FlareSolverr 改回「填 URL 即用」，删除内置 FlareSolverr 的两个 compose（`docker-compose.hub.yml` / `docker-compose.flaresolverr.yml`）；`install.sh`/`install.ps1` 改为只装本体 | compose / 脚本 / README |
| 防过载死机：`_fsgate.py` 重写为串行闸 + 排队上限背压 + 连续失败熔断 + 最小间隔 | `backend/scrapers/_fsgate.py` |
| 修复 JavBus+JavDB 合并卡详情页样品图/磁力（补 `source_urls`） | `frontend/index.html`、`backend/scrapers/__init__.py` |
| FlareSolverr 地址留空＝自动探测（常见地址 + /24 网段扫描，正/负缓存防重扫）；JavDB/FC2/MissAV 三源接入；连通诊断回显探到的地址 | `backend/scrapers/_fsgate.py`、`javdb.py`、`fc2.py`、`_missav.py`、`__init__.py`、`frontend/index.html` |

## FC2-PPV 数据源

新增 **FC2-PPV 专用数据源**（fc2ppvdb.com），把 FC2 素人/无码片源纳入检索与刮削，
番号、标题、封面、女优、卖家、贩卖日、收录时间、标签一并抓取。

---

## 为什么单独做 FC2

FC2-PPV 是独立番号体系（如 `FC2-PPV-1234567`），此前只有 JavDB 偶尔顺带收录少量，
搜全率和字段完整度都很差。本版接入 **fc2ppvdb.com**——FC2-PPV 专用数据库，字段最规整。

fc2ppvdb 与 JavBus 系（avsox/avmoo 复用 `_javbus_base`）页面模板完全不同（Laravel + Tailwind），
因此单独写了 [`backend/scrapers/fc2.py`](backend/scrapers/fc2.py)，未复用 JavBus 模板。

---

## 关键点：必须配 FlareSolverr

fc2ppvdb **强制 Cloudflare Turnstile 人机验证**，直连只能拿到验证页（实测直连仅返回
Turnstile 挑战页 + 登录表单）。因此 FC2 取页复用了 JavDB 那套
「FlareSolverr 优先 + 增强 httpx 兜底」的策略：

- **FC2 的 FlareSolverr 地址留空时，自动复用 JavDB 已填的 `javdb_flaresolverr_url`**，
  已经为 JavDB 配过 FlareSolverr 的用户无需重复填写。
- 也可在「设置 → FC2 数据源（1.4.3）」单独指定 FC2 专用 FlareSolverr 地址、是否复用主代理、
  可选手动 Cookie。

---

## 番号识别

以下写法都会被识别为「番号检索」，直接命中 `fc2ppvdb.com/articles/{番号}` 详情页（最稳最快）：

- `FC2-PPV-1234567`
- `FC2PPV1234567`
- `FC2-1234567`
- 纯数字 `1234567`（6–7 位）

关键词 / 女优搜索走站内搜索 `fc2ppvdb.com/search?stext=...`。

---

## 下载链路说明（重要）

fc2ppvdb **不提供磁力链**。检索（元数据）与抓取（磁力/种子）是两条链路：

- 元数据：本版的 FC2 数据源负责。
- 下载：仍走你现有的 **Jackett / sukebei**，用番号 `FC2-PPV-xxxxxxx` 检索后推送 qBittorrent。

卡片上的「Jackett 搜索 / 推送下载」按钮对 FC2 同样可用，番号已统一规范成 `FC2-PPV-xxxxxxx`
（sukebei 最通用的写法）。

---

## 使用步骤

1. 设置 → 常规 → 数据源，勾选 **FC2**。
2. 设置 → 「FC2 数据源（1.4.3）」：FlareSolverr 地址留空即复用 JavDB 的；如未配过，填一个
   FlareSolverr 地址（如 `http://192.168.1.100:8191`）。
3. 点「测试 FC2 连通」确认可达、未被 Turnstile 拦截。
4. 搜索框输入 `FC2-PPV-1234567` 或女优名 → 出现 FC2 结果卡片。
5. （可选）首页最新片源里勾上 FC2；FC2 走 FlareSolverr 单页较慢，数量建议 30 条左右。

---

## 改动清单

| 文件 | 改动 |
|------|------|
| `backend/scrapers/fc2.py` | **新增**：FC2 刮削器（列表/详情/最新/连通诊断，FlareSolverr 取页） |
| `backend/scrapers/__init__.py` | 注册 `fc2` 数据源、合并优先级；`detect_search_mode` 增加 FC2 番号识别 |
| `backend/config_manager.py` | 新增 `fc2_flaresolverr_url` / `fc2_flaresolverr_use_proxy` / `fc2_cookie`；`latest_limits` 增加 `fc2` |
| `backend/main.py` | 版本号 1.4.3；配置模型与脱敏增加 FC2 字段；新增 `GET /api/fc2/test` 诊断接口；新增 `GET /api/version`（版本检测，带缓存/代理）；`GET /api/latest` 支持 `?source=` 单源抓取 |
| `frontend/index.html` | 数据源勾选增加 FC2；新增「FC2 数据源」设置组；首页最新数量增加 FC2；**顶部版本号 + 更新红点**；**最新片源各源并行边抓边显示**；**数据源切换标签**；读写配置联动 |
| `README.md` | 数据源说明、项目结构、API 表、更新日志 |

> 实现要点：前端列表渲染为「主索引」模式（`currentResults` 为全量主数组，详情按主索引补全）。
> 数据源筛选引入 `viewIndices`（过滤后可见的主索引列表），分页/补全/翻页全部基于它，
> 因此**过滤不会破坏详情补全的索引映射**。边抓边合并只新增/补字段、不重排，索引稳定。

---

## 已知限制

- FC2 详情的**样品图**仅做低噪声的「图片链接」抓取（lightbox 形式），部分页面可能为空；
  封面来自 fc2ppvdb 缩略图。
- fc2ppvdb 页面结构若调整，标签字段（女优/贩卖日等）采用「文本前缀 + span」提取，
  对 Tailwind class 变动免疫，但若站点改版仍可能需要微调选择器。

---

## v1.4.2（2026-06-05）

本版本聚焦解决「勾选了 JavDB 却没有任何结果」的问题，打通 JavDB 抓取链路，并扩充信息量、规范多来源合并展示与下载推送体验。

## 背景

1.4.1 的 JavDB 刮削器只用了裸 `httpx` + 固定 UA，**既不过 Cloudflare 盾，也不带年龄确认 Cookie**。JavDB 风控较严，常返回 challenge 页 / 年龄门 / 地区封禁页（HTTP 200 但页面里没有片源卡片），解析为 0 条且错误被静默吞掉，于是 UI 上「悄悄消失」——看不到 JavDB，并非被同片合并隐藏（合并会把所有命中来源记进 `sources` 并显示成 `JavBus+JavDB`）。

## 一、JavDB 抓取链路打通

- **反爬增强**：自动携带 `over18`/`locale`/`theme` Cookie 绕过年龄门，补全完整浏览器请求头（`Sec-Fetch-*`、`sec-ch-ua` 等），失败自动重试并识别 CF 验证页。
- **可选 FlareSolverr**：配置 `FlareSolverr 地址` 后，JavDB 请求改走 FlareSolverr 取过盾后的 HTML（含 `cf_clearance`）。
  - Cookie 仅以 `name/value` 形式传入（带 `domain` 会令部分版本报 500）；带 Cookie 失败时自动重试一次不带 Cookie。
  - **代理账号密码自动拆分**：`http://user:pass@host:port` 会拆成 `url`+`username`+`password` 传给 FlareSolverr，解决其内置 Chrome 报 `ERR_NO_SUPPORTED_PROXIES`。
  - 新增开关 **「FlareSolverr 复用代理」**（默认开）：FlareSolverr 自带网络出口（如 WARP）时可关掉走直连。
- **手动 Cookie**：可在设置填入浏览器导出的 JavDB Cookie（如 `cf_clearance=...`），直连也能过盾。该字段读取时脱敏。

> 重要事实：**JavDB 因版权封禁「日本本土 IP」**。请使用非日本节点（美国/香港/台湾/新加坡等）的住宅 IP；机房 IP 也可能另被 CF 验证。

## 二、连通诊断（设置页「测试 JavDB 连通」）

新接口 `GET /api/javdb/test`，返回并在界面展示：
- 是否可达 / 是否命中 CF 盾 / HTTP 状态 / 解析条数 / 取页方式（flaresolverr / httpx）；
- **出口 IP 所在国家**（经同一代理查 ip-api，判断是否日本/机房 IP）；
- 0 条时**回显实际页面**的标题与片段，并判定页面类型：`年龄门 / 登录墙 / 地区封禁 / 人机验证 / 代理错误(ERR_NO_SUPPORTED_PROXIES)`，给出针对性建议；
- 出口 IP 探测失败时提示「代理本身不通/不稳」。

## 三、抓取信息扩充与合并规范

- JavDB 详情新增字段：**磁力链 `magnets`**（名称/大小/日期/高清/字幕标记）、**样品图 `samples`**、**评分人数 `score_count`**，演员/片商/发行/系列解析更稳。
- 合并去重（`_merge_lists`）：以高优先级来源（JavBus）为主条目保留封面/标题，用其它来源补全缺失字段（含 `score_count`/`actors`/`tags`/`samples`/`magnets`），命中来源全部记入 `sources`，展示为 `JavBus / JavDB`，规范不变。
- 合并条目记录各来源的详情页地址 `source_urls`，用于按需补抓非主来源的样品图/磁力。

### JavDB 样品图/磁力的显示
- 合并卡（JavBus+JavDB）主来源是 JavBus，**点开详情时**后台用 `source_urls['JavDB']` 补抓 JavDB 详情，仅并入样品图/磁力等独有字段并局部刷新该区块（不覆盖封面标题、不影响已翻译内容）。
- 新增开关 **「样品图预取」**（默认关）：开启后翻页预取时顺带提前抓好合并卡的 JavDB 样品图/磁力，点开即时显示，代价是 FlareSolverr 请求更多。

## 四、磁力下载推送

- JavDB 详情每条磁力新增 **推送下载(qB) / 迅雷 / 复制磁链** 按钮，与磁力信息同一行（靠右）。
- **推送到 qBittorrent 前自动给磁力补公共 tracker**：JavDB 多为「只有 hash 无 tracker」的裸磁力，qB 仅靠 DHT 在 NAT 下常卡在「下载元数据」0 数据；补 12 个稳定公共 tracker 后显著改善。
  - 若个别仍卡住：检查 qB 已启用 DHT/PeX/LSD、监听端口已转发、或该磁力确无活跃做种者。

## 五、刮削番号识别（按可靠度三级递进）

修复「推送下载后刮削番号标记不准」的问题。识别顺序：

1. **推送下载（Jackett / JavDB 磁力）**：推送时记录「番号 + 磁力 infohash」；刮削时查 qB API 找到该文件对应种子的 infohash，**精确反查记录的番号**（即详情页的字母+数字番号）。不依赖文件名，落地目录名再乱也能命中。
2. **推送兜底**：infohash 未命中时，按文件名/目录名与推送记录（番号/显示名）匹配反查。
3. **迅雷下载 / 手动复制到监控目录**（无推送标记）：直接按文件名正则识别番号——已剔除站点前缀（`hhd800.com@`）、`[标签]`、分集后缀（`-C`）等噪声，且不会把 `2024`/`1080p` 误判为番号。

附带：推送记录去重改为「仅按相同 infohash 去重」，同一影片推送多个磁力版本时，每个都能按各自 infohash 反查到正确番号。

## 六、浏览体验与性能

- **封面完整显示**：列表/首页卡片封面由 `object-fit: cover`（裁切）改为 `contain`，按布局等比显示完整封面。
- **单源超时**：搜索每个来源 25s、首页最新 40s，慢源（JavDB/FlareSolverr）超时即丢弃，不拖累其它来源合并返回。
- **FlareSolverr 限页**：JavDB 走 FlareSolverr 时列表只抓 1 页（约 28-40 条），避免多页累加超时；`maxTimeout` 40s。
- **详情预取窗口**：翻页后台串行预取后续 3 页详情，翻页更顺。
- **图片代理加固**：内存缓存 200→800，单次超时 20→12s，新增并发上限 12，避免一页多图把代理打满导致集体超时。

## 新增/变更配置项（`config/settings.json`）

```jsonc
{
  "javdb_flaresolverr_url": "",          // FlareSolverr 地址，留空走增强直连
  "javdb_flaresolverr_use_proxy": true,  // FlareSolverr 是否复用主代理；自带出口时关掉走直连
  "javdb_cookie": "",                    // 可选，浏览器导出的 JavDB Cookie（脱敏存储）
  "javdb_prefetch_extras": false         // 翻页时是否预取合并卡的 JavDB 样品图/磁力（默认关）
}
```

## 测试建议

1. 设置 →「JavDB 反爬」→ 填 FlareSolverr 地址（如有）→「测试 JavDB 连通」。
2. 按结论处理：被 CF/年龄门 → 配 FlareSolverr/Cookie；`ERR_NO_SUPPORTED_PROXIES` → 关「复用代理」或换无认证代理；地区封禁/日本 IP → 换非日本住宅节点。
3. 显示「✓ 可正常访问 + N 条」后搜索/看首页，确认出现 JavDB / `JavBus+JavDB` 卡片，点开详情看样品图与磁力，并测试推送到 qB。

---

## v1.4.1（2026-06-04）

在 V1.4 基础上修复两个问题，不改变既有功能与配置。

## 1. 番号识别更准确（修复误判）

**问题**：刮削时仅靠文件名用「字母+数字」正则猜番号，遇到带站点/数字前缀的文件名会误判。例如
`hhd800.com@390JAC-234.mp4` 的真实番号是 `390JAC-234`，却被识别成 `HHD-800`，导致搜索元数据失败。

**修复**：

- **推送即标记（主修复）**：在前端搜索结果里点「推送下载」时，那条影片的番号是准确的。推送时会把
  「番号 ↔ 该磁力/种子特征」记录到 `config/push_hints.json`；下载完成刮削时优先用文件名（及其上级
  目录名）反查这份标记，命中则直接采用准确番号，未命中再回退文件名解析。
- **文件名正则增强（兜底）**：解析前先剔除 `hhd800.com@`、`[javbus.com]`、`www.xxx.cc-` 等站点/广告
  前缀；并新增对 `390JAC-234`、`259LUXU-1234` 这类「数字前缀番号」的支持。

> 对手动放入（未经推送）的文件，靠增强后的文件名解析；经本程序推送下载的文件，番号识别最准。

## 2. 服务地址填 `http://localhost` 时种子下载/外网访问修复

**问题**：相关服务地址写成 `http://localhost` 后，连通检测、推送、磁力链均正常，但 Jackett 返回的
`.torrent` 直链带着它自身地址（`http://localhost:9117/...`），外网点击打不开，推送种子（无磁力时）也会失败。

**修复**：种子直链统一改为「由后端代取、再转交」——

- 前端「种子」按钮指向后端新接口 `/api/jackett/download`，后端取回 `.torrent` 后以附件回传浏览器
  （文件名自动用番号），任意网络都能下载。
- 推送 qBittorrent 时：磁力链照旧直接交给 qB；`.torrent` 直链由后端先取回字节再以文件上传给 qB，
  qB 端不再碰到 `localhost`。
- 迅雷链接在仅有种子时也改用后端代理的绝对地址。

## 涉及文件

- 新增 `backend/push_hints.py`
- `backend/library.py`、`backend/qbittorrent.py`、`backend/main.py`、`frontend/index.html`
