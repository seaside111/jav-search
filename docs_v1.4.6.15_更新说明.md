# V1.4.6.15 更新说明

- 修复单片 `番号-C/c` 被误判为多段视频并改名为 `CD3` 的问题。
- 完善多段视频连续性、广告过滤、移动归档和自定义文件名规则，避免规则互相覆盖。
- 新增唯一番号 `番号-C/c` 的中文硬字幕识别，并写入 NFO 标签及封面角标。
- 优化 qBittorrent / Transmission 无临时后缀文件的完成判断，优先使用下载器 API。
- 增加 API 查询失败保护、任务路径匹配增强，以及非下载器文件的稳定性兜底。
